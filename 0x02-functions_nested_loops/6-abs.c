#include "main.h"

/**
 * _abs - check the code
 *
 * @n: n is an integer
 *
 * Return: Always n ou -n ou n.
 */

int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
